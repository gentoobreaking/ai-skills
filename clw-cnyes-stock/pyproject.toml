[project]
name = "cnyes-stock-scraper"
version = "1.0.0"
description = "鉅亨網台股新聞自動抓取"
requires-python = ">=3.10"
dependencies = ["playwright>=1.40.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cnyes_stock_scraper"]
